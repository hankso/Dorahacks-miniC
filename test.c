#include <stdio.h>

int tmp(int b, int c) { printf("%d", b); return c; }

int main()
{ int a = 1 + b; printf("hello world"); a = tmp(b, a); printf("%d", a);
  float *e, *f; *e = 99; *f = *e * *e;
    struct aaaaa { int tt; double rr; };
    struct aaaaa aa[5]; int d; int c = 0;
    for (; c<10; c++) { printf("-+%d+-", c); }
    if (a == 3.0) { printf("a equals to 3.0"); }
    while (a > 0) { a--; printf("current a: %d", a); }
    return 0;
}
